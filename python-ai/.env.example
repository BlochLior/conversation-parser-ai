# .env.example — copy this to .env and update values

# 🔐 Required: Your OpenAI API key (starts with sk-...)
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 🧠 Optional: Model to use (must be accessible by your API key)
# Recommended: gpt-3.5-turbo, or gpt-4-turbo if available
OPENAI_MODEL=gpt-3.5-turbo

# 🌎 Optional: Set ENV=production to disable dotenv loading in Docker
ENV=development
